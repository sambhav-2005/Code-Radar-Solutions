#include<stdio.h>

void duplicateElement(int arr[], int size)
{
    for(int i = 0; i < size; i++)
    {

    }
}

int main(void)
{
    int arr[100];
    int size;
    scanf("%d", &size);

    for(int i = 0; i < size;i++)
    {
        scanf("%d", &arr[i]);

    }

    duplicateElement(arr, size);

    return 0;
}