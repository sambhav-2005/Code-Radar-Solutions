int findOccurence(arr, n, target, mode)
{
    int start = 0;
    int end = size - 1;
    int mid = start + (end - start)/2;
    int answer = 0;
    
    if(target == mid)
    {
        return arr[mid];
    }
    
}