#include <stdio.h>

int main(void)
{
    int n;
    scanf("%d", &n);
    int i = 0;
    while(i <= n)
    {
        int j = 0;
        while(i <= n - i)
        {
            printf("*"); 
        }
    }
}